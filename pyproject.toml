[project]
name = "scripts"
version = "0.1.0"
description = "Reusable scripts for documentation and development workflows"
requires-python = ">=3.11"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.6.20",
]
